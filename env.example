# Backend API Base URL
# Leave empty for local development (uses Vite dev server proxy to localhost:8000)
# Set to the deployed backend URL in production (e.g. https://sts-backend-xyz.a.run.app)
VITE_API_BASE_URL=
